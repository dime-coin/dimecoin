# List of fixed seed nodes for testnet
#
# No Dimecoin testnet onion seeds are currently available.
# Entries below were stale Bitcoin testnet .onion addresses and have been
# removed. Keep this list empty (or comment-only) until real Dimecoin
# testnet seeds exist, so the generator emits an empty pnSeed6_test[] block.

